h1 Project Edit:

form.user-form(action="/projects/#{id}", method='post')
    != window.Training.Helpers.csrfToken()
    input(name="_method", type="hidden", value="put")

    .form-group
        label(for='project_name') Project name:
        input#project_name.form-control(type='text', name='project[name]', value=name)

    .form-group
        label(for='project_status') Project status:
        select#project_status.form-control(name='project[status]')
            statuses = ['inprogress','complete','failed']
            for value in statuses
                option(value=value, selected=value == status) #{value}

    .form-group
        label(for='project_level') Project level:
        select#project_level.form-control(name='project[level]')
            levels = ['easy', 'hard', 'very hard', 'unknown']
            for value in levels
                option(value=value, selected=value == status) #{value}

    .form-group.display-inline.reg-mg-right
        input.btn.btn-success(type="submit", value='Update')
    .back.display-inline
        a.btn.btn-danger(href=Routes.dashboard_index_path()) Cancel